<div class="title container d-flex justify-content-center">
<h3><%=t('.title')%></h3>
</div>

<%= render 'form', subtype: @subtype %>

<%= link_to type_subtypes_path(@type) do%>
<i class="fas fa-undo"> <%= t('back') %></i>
<%end%>
</div>
</div>
</div>