<%= form_with(model: timeblock, local: true) do |form| %>
  <% if timeblock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(timeblock.errors.count, "error") %> prohibited this timeblock from being saved:</h2>

      <ul>
        <% timeblock.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.time_select :start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.time_select :end_time %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
